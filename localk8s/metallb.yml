apiVersion: v1
kind: ConfigMap
metadata:
  namespace: metallb-system
  name: config
data:
  config: |
    address-pools:
    - name: arcus-ip-space
      protocol: layer2
      addresses:
      - PLACEHOLDER_1
